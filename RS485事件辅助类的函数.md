## 1. 目标
本文档用于说明 RS485 / Modbus-RTU 事件辅助类的实现过程。该辅助类负责串口收发、中断事件、帧间隔判断、CRC 校验，以及根据 `Modbus-RTU 主从通信任务流程.md` 中的寄存器映射更新 IMU、触觉电阻点阵、通信和日志状态。

实现目标：

```plain
1. RS485 使用 2M 波特率，降低高频采集通信耗时。
2. 接收使用 UART RX DMA + IDLE 中断。
3. 发送使用 UART TX DMA，并在 DMA 完成中断中关闭 RS485 发送使能。
4. 3.5 个字符时间的定时器作为 Modbus-RTU 帧间隔兜底或调试校验。
5. 提供 IMU 和触觉电阻点阵事件回传函数。
6. 根据寄存器读写请求更新或反馈对应状态。
```

---

## 2. 串口与时序配置

### 2.1 波特率

```plain
BaudRate = 2,000,000 bps
数据格式建议：8N1
1 个字符时间约等于 10 bit 时间
```

字符时间：

```plain
Tchar = 10 / 2,000,000 = 5 us
T3.5  = 3.5 × Tchar = 17.5 us
```

主接收成帧依赖 UART IDLE 中断。3.5 字符定时器可以设置为 `18 us ~ 20 us`，作为 Modbus-RTU 帧间隔兜底、调试统计或异常校验；如果 IDLE 中断稳定可用，定时器不必作为主成帧入口。

---

## 3. 核心状态对象

建议维护一个 RS485 辅助对象：

```c
typedef struct
{
    uint8_t  rx_buf[MODBUS_RX_BUF_SIZE];
    uint16_t rx_len;
    uint8_t  rx_pending;
    uint8_t  frame_ready;

    uint8_t  tx_buf[MODBUS_TX_BUF_SIZE];
    uint16_t tx_len;
    uint8_t  tx_busy;

    uint16_t imu_status_bits;      // REG_IMU_STATUS_BITS, 0x1144
    uint64_t imu_timestamp_us;     // REG_IMU_TIMESTAMP_US, 0x1140 ~ 0x1143
    uint16_t r_status_regs[4];     // REG_R_STATUS_START ~ END, 0x2084 ~ 0x2087
    uint64_t touch_timestamp_us;   // REG_R_TIMESTAMP_US, 0x2080 ~ 0x2083
    uint16_t sensor_state;         // REG_SENSOR_STATE, 0x0044
    uint16_t comm_state;           // REG_COMM_STATE, 0x0045
} RS485_EventHelper_t;
```

时间戳绑定关系：

```plain
imu_timestamp_us:
    绑定 REG_IMU_TIMESTAMP_US 0x1140 ~ 0x1143
    对应当前 IMU 数据帧时间戳。

touch_timestamp_us:
    绑定 REG_R_TIMESTAMP_US 0x2080 ~ 0x2083
    对应当前触觉电阻点阵数据帧时间戳。
```

状态位约定按协议寄存器表：

```plain
IMU 状态：
bit = 1：对应 IMU 正常
bit = 0：对应 IMU 异常或离线

触觉电阻状态：
bit = 1：对应电阻点正常
bit = 0：对应电阻点异常、离线或数据无效
```

---

## 4. 事件标志定义

事件标志分两类使用：

```plain
1. RS485Task 消息事件：用于任务调度，例如 RX DMA IDLE 成帧、DMA 发送完成、3.5 字符超时兜底。
2. 状态回传事件：用于更新寄存器缓存，例如 IMU 状态、电阻点状态、SD 状态、通信错误状态。
```

### 4.1 RS485Task 消息事件标志

建议用于 RTOS EventGroup、消息队列或裸机事件位：

```c
#define RS485_EVT_RX_DMA_IDLE         (1U << 0)  // RX DMA + UART IDLE 触发，一帧结束
#define RS485_EVT_FRAME_TIMEOUT       (1U << 1)  // 3.5 字符定时器超时兜底
#define RS485_EVT_FRAME_READY         (1U << 2)  // Modbus 帧可解析
#define RS485_EVT_TX_DMA_DONE         (1U << 3)  // TX DMA 发送完成
#define RS485_EVT_TX_TC_DONE          (1U << 4)  // UART TC 完成，可切回接收
#define RS485_EVT_PARSE_REQUEST       (1U << 5)  // 解析 Modbus 请求
#define RS485_EVT_SEND_RESPONSE       (1U << 6)  // 发送 Modbus 回复
#define RS485_EVT_ERROR               (1U << 7)  // 通信错误事件
```

### 4.2 Modbus 协议事件标志

根据功能码和地址分发：

```c
#define RS485_EVT_MB_READ_REGS        (1U << 8)   // 功能码 0x03
#define RS485_EVT_MB_WRITE_SINGLE     (1U << 9)   // 功能码 0x06
#define RS485_EVT_MB_WRITE_MULTI      (1U << 10)  // 功能码 0x10
#define RS485_EVT_MB_ADDR_DISCOVERY   (1U << 11)  // 地址发现：读 0x0000
#define RS485_EVT_MB_WRITE_UTC        (1U << 12)  // 写 REG_TIME_SYNC_UTC_US, 0x000A
#define RS485_EVT_MB_WRITE_CMD        (1U << 13)  // 写 REG_CMD, 0x0020
#define RS485_EVT_MB_WRITE_LOG_CTRL   (1U << 14)  // 写日志控制相关寄存器或命令
#define RS485_EVT_MB_WRITE_IMU_OFFSET (1U << 15)  // 写 IMU Offset, 0x1154 ~ 0x1293
```

### 4.3 传感器状态事件类型

用于 `IMU_Event_t` 和 `Touch_Event_t` 中的 `event_type`：

```c
#define EVENT_IMU_STATUS              0x1001  // 更新 REG_IMU_STATUS_BITS, 0x1144
#define EVENT_IMU_DATA_VALID          0x1002  // IMU 数据有效性事件
#define EVENT_IMU_SAMPLE_ERROR        0x1003  // IMU 采样错误事件

#define EVENT_TOUCH_STATUS            0x2001  // 更新 REG_R_STATUS_START ~ END, 0x2084 ~ 0x2087
#define EVENT_TOUCH_DATA_VALID        0x2002  // 触觉电阻数据有效性事件
#define EVENT_TOUCH_SAMPLE_ERROR      0x2003  // 触觉电阻采样错误事件
```

状态参数统一建议：

```c
#define SENSOR_STATUS_ERROR           0U
#define SENSOR_STATUS_OK              1U
#define SENSOR_ID_ALL                 0xFF
```

### 4.4 SD 与日志事件标志

对应 SD 状态区和日志控制寄存器：

```c
#define RS485_EVT_SD_FS_CHANGED       (1U << 16)  // REG_SD_FS_STATUS, 0x0081
#define RS485_EVT_SD_LOG_CHANGED      (1U << 17)  // REG_SD_LOG_STATUS, 0x0082
#define RS485_EVT_SD_ERROR_CHANGED    (1U << 18)  // REG_SD_ERROR_CODE, 0x0083
#define RS485_EVT_LOG_CREATE_FILE     (1U << 19)  // CMD_LOG_CREATE_FILE, 0x0092
#define RS485_EVT_CMD_LOG_START       (1U << 20)  // CMD_LOG_START, 0x0094
#define RS485_EVT_CMD_LOG_STOP        (1U << 21)  // CMD_LOG_STOP, 0x0096
#define RS485_EVT_LOG_LENGTH_QUERY    (1U << 22)  // CMD_LOG_LENGTH, 0x009A
```

### 4.5 命令寄存器事件标志

采集控制、日志开始/停止、SD 文件列表和下载统一通过命令寄存器区触发。

```c
#define RS485_EVT_CMD_ACQ_START       (1U << 23)  // CMD_ACQ_START, 0x0501
#define RS485_EVT_CMD_ACQ_STOP        (1U << 24)  // CMD_ACQ_STOP, 0x0502
#define RS485_EVT_WORK_STATE_CHANGED  (1U << 25)  // REG_WORK_STATE, 0x0500
#define RS485_EVT_CMD_SD_LIST_FILES   (1U << 26)  // CMD_SD_LIST_FILES，命令字待重新分配
#define RS485_EVT_CMD_SD_DOWNLOAD     (1U << 27)  // CMD_SD_DOWNLOAD_FILE，命令字待重新分配
#define RS485_EVT_CMD_ACK_CHANGED     (1U << 28)  // REG_CMD_ACK, 0x0023
```

### 4.6 通信错误事件标志

用于更新 `REG_COMM_STATE 0x0045`：

```c
#define RS485_COMM_OK                 0x0000
#define RS485_COMM_CRC_ERROR          (1U << 0)
#define RS485_COMM_ADDR_ERROR         (1U << 1)
#define RS485_COMM_FUNC_ERROR         (1U << 2)
#define RS485_COMM_REG_ERROR          (1U << 3)
#define RS485_COMM_LEN_ERROR          (1U << 4)
#define RS485_COMM_RX_OVERFLOW        (1U << 5)
#define RS485_COMM_TX_BUSY            (1U << 6)
#define RS485_COMM_TIMEOUT            (1U << 7)
#define RS485_COMM_TX_ERROR           (1U << 8)
```

### 4.7 事件到寄存器的映射关系

```plain
EVENT_IMU_STATUS:
    更新 REG_IMU_STATUS_BITS 0x1144

IMU 数据帧刷新:
    更新 REG_IMU_TIMESTAMP_US 0x1140 ~ 0x1143

EVENT_TOUCH_STATUS:
    更新 REG_R_STATUS_START ~ REG_R_STATUS_END 0x2084 ~ 0x2087

触觉电阻数据帧刷新:
    更新 REG_R_TIMESTAMP_US 0x2080 ~ 0x2083

RS485_COMM_*:
    更新 REG_COMM_STATE 0x0045

RS485_EVT_SD_* / RS485_EVT_LOG_*:
    更新 REG_SD_FS_STATUS、REG_SD_LOG_STATUS、REG_SD_ERROR_CODE 或日志控制状态

RS485_EVT_CMD_ACQ_START:
    写 REG_CMD = CMD_ACQ_START 0x0501 触发开始采集，执行后更新 REG_WORK_STATE 0x0500

RS485_EVT_CMD_ACQ_STOP:
    写 REG_CMD = CMD_ACQ_STOP 0x0502 触发结束采集，执行后更新 REG_WORK_STATE 0x0500

RS485_EVT_CMD_LOG_START / RS485_EVT_CMD_LOG_STOP:
    写 REG_CMD = CMD_LOG_START 0x0094 或 CMD_LOG_STOP 0x0096，执行后更新 REG_SD_LOG_STATUS 0x0082

RS485_EVT_CMD_*:
    更新 REG_CMD_ACK 0x0023、REG_CMD_ACK_SEQ 0x0024、REG_CMD_ERROR 0x0025，并触发对应任务处理
```

---

## 5. 事件回传函数

事件回传函数建议统一传结构体指针，而不是把 `event_type / id / status / timestamp` 拆成多个参数。这样后续扩展错误码、时间戳、数据来源时，不需要频繁修改函数签名。

栈占用建议：

```plain
1. 函数形参只传结构体指针，栈上只增加一个指针大小。
2. 事件结构体建议由调用方静态分配、全局分配，或放入消息队列对象池。
3. 不建议在频繁中断或高频任务里临时创建很大的局部结构体。
```

公共定义已放到统一头文件：[rs485_protocol_common.h](</d:/giteeproject/glovesV1.0_VGT6/Services/inc/rs485_protocol_common.h>)。核心内容如下：

```c
#ifndef RS485_PROTOCOL_COMMON_H
#define RS485_PROTOCOL_COMMON_H

#include <stdint.h>

/* 关键寄存器地址 */
#define REG_CMD                     0x0020
#define REG_CMD_PARAM               0x0021
#define REG_CMD_SEQ                 0x0022
#define REG_CMD_ACK                 0x0023
#define REG_CMD_ACK_SEQ             0x0024
#define REG_CMD_ERROR               0x0025

#define REG_SENSOR_STATE            0x0044
#define REG_COMM_STATE              0x0045

#define REG_WORK_STATE              0x0500

#define REG_SD_FS_STATUS            0x0081
#define REG_SD_LOG_STATUS           0x0082
#define REG_SD_ERROR_CODE           0x0083
#define CMD_LOG_CREATE_FILE         0x0092
#define CMD_LOG_START               0x0094
#define CMD_LOG_STOP                0x0096
#define CMD_LOG_LENGTH              0x009A

#define CMD_ACQ_START               0x0501
#define CMD_ACQ_STOP                0x0502

/* Modbus 异常码 */
#define MB_EX_ILLEGAL_FUNCTION      0x01
#define MB_EX_ILLEGAL_DATA_ADDRESS  0x02
#define MB_EX_ILLEGAL_DATA_VALUE    0x03
#define MB_EX_SLAVE_DEVICE_FAILURE  0x04
#define MB_EX_SLAVE_DEVICE_BUSY     0x06

/* 命令 ACK */
#define CMD_ACK_IDLE                0x0000
#define CMD_ACK_OK                  0x0001
#define CMD_ACK_BUSY                0x0002
#define CMD_ACK_UNKNOWN_CMD         0x8001
#define CMD_ACK_PARAM_ERROR         0x8002
#define CMD_ACK_STATE_DENIED        0x8003
#define CMD_ACK_FAILED              0x8004

/* 命令错误原因 */
#define CMD_ERR_NONE                0x0000
#define CMD_ERR_BAD_PARAM           0x0001
#define CMD_ERR_SEQ_EXPIRED         0x0002
#define CMD_ERR_STATE_DENIED        0x0003
#define CMD_ERR_RESOURCE_NOT_READY  0x0004
#define CMD_ERR_START_FAILED        0x0005
#define CMD_ERR_STOP_FAILED         0x0006
#define CMD_ERR_TIMEOUT             0x0007

#define REG_IMU_TIMESTAMP_US        0x1140
#define REG_IMU_STATUS_BITS         0x1144
#define REG_IMU_OFFSET_START        0x1154
#define REG_IMU_OFFSET_END          0x1293

#define REG_R_TIMESTAMP_US          0x2080
#define REG_R_STATUS_START          0x2084
#define REG_R_STATUS_END            0x2087

/* 事件类型 */
#define EVENT_IMU_STATUS            0x1001
#define EVENT_IMU_DATA_VALID        0x1002
#define EVENT_IMU_SAMPLE_ERROR      0x1003

#define EVENT_TOUCH_STATUS          0x2001
#define EVENT_TOUCH_DATA_VALID      0x2002
#define EVENT_TOUCH_SAMPLE_ERROR    0x2003

#define SENSOR_STATUS_ERROR         0U
#define SENSOR_STATUS_OK            1U
#define SENSOR_ID_ALL               0xFF

typedef struct
{
    uint16_t event_type;
    uint8_t  imu_id;        /* 0 ~ 15，SENSOR_ID_ALL 表示全部 IMU */
    uint8_t  reserved;
    uint16_t status;        /* 1 = 正常，0 = 异常，后续可扩展错误码 */
    uint16_t error_code;    /* 可选：详细错误码 */
    uint64_t timestamp_us;  /* 绑定 REG_IMU_TIMESTAMP_US */
} IMU_Event_t;

typedef struct
{
    uint16_t event_type;
    uint8_t  touch_id;      /* 0 ~ 63，SENSOR_ID_ALL 表示全部触觉电阻点 */
    uint8_t  reserved;
    uint16_t status;        /* 1 = 正常，0 = 异常，后续可扩展错误码 */
    uint16_t error_code;    /* 可选：详细错误码 */
    uint64_t timestamp_us;  /* 绑定 REG_R_TIMESTAMP_US */
} Touch_Event_t;

#endif
```

### 5.1 IMU_setEvent

函数原型建议：

```c
void IMU_setEvent(const IMU_Event_t *event);
```

参数说明：

```plain
event->event_type：事件类型，例如在线状态、数据有效性、校准状态、采样错误等。
event->imu_id：IMU 编号，范围 0 ~ 15。
event->status：16 位状态值；当前状态位建议使用 1 = 正常，0 = 异常，后续可扩展错误码。
event->timestamp_us：本次 IMU 状态或数据帧对应的时间戳，绑定 REG_IMU_TIMESTAMP_US。
```

特殊约定：

```plain
event->imu_id = SENSOR_ID_ALL 表示设置全部 IMU。
event->status = 1 表示全部或指定 IMU 正常。
event->status = 0 表示全部或指定 IMU 异常。
```

典型操作：

```plain
IMU 无异常：
    IMU_Event_t event = {
        .event_type = EVENT_IMU_STATUS,
        .imu_id = SENSOR_ID_ALL,
        .status = SENSOR_STATUS_OK,
        .timestamp_us = imu_frame_timestamp_us,
    };
    IMU_setEvent(&event);

指定 IMU 异常：
    IMU_Event_t event = {
        .event_type = EVENT_IMU_STATUS,
        .imu_id = imu_id,
        .status = SENSOR_STATUS_ERROR,
        .error_code = imu_error_code,
        .timestamp_us = imu_frame_timestamp_us,
    };
    IMU_setEvent(&event);
```

寄存器更新规则：

```plain
REG_IMU_STATUS_BITS 0x1144：
    bit0  = IMU0
    bit1  = IMU1
    ...
    bit15 = IMU15

event->status = 1：置位对应 bit
event->status = 0：清零对应 bit

REG_IMU_TIMESTAMP_US 0x1140 ~ 0x1143：
    event->timestamp_us 写入 g_rs485.imu_timestamp_us。
```

伪代码：

```c
void IMU_setEvent(const IMU_Event_t *event)
{
    if (event == NULL)
    {
        return;
    }

    g_rs485.imu_timestamp_us = event->timestamp_us;

    if (event->event_type != EVENT_IMU_STATUS)
    {
        return;
    }

    if (event->imu_id == SENSOR_ID_ALL)
    {
        g_rs485.imu_status_bits = event->status ? 0xFFFF : 0x0000;
        return;
    }

    if (event->imu_id >= 16)
    {
        return;
    }

    if (event->status)
    {
        g_rs485.imu_status_bits |= (uint16_t)(1U << event->imu_id);
    }
    else
    {
        g_rs485.imu_status_bits &= (uint16_t)~(1U << event->imu_id);
    }
}
```

---

### 5.2 触觉_setEvent

函数原型建议：

```c
void Touch_setEvent(const Touch_Event_t *event);
```

如果代码中需要中文命名，也可以封装为：

```c
void 触觉_setEvent(const Touch_Event_t *event);
```

参数说明：

```plain
event->event_type：事件类型，例如触觉电阻状态、数据有效性、采样错误等。
event->touch_id：触觉电阻编号，范围 0 ~ 63。
event->status：16 位状态值；当前状态位建议使用 1 = 正常，0 = 异常，后续可扩展错误码。
event->timestamp_us：本次触觉电阻数据帧对应的时间戳，绑定 REG_R_TIMESTAMP_US。
```

特殊约定：

```plain
event->touch_id = SENSOR_ID_ALL 表示设置全部触觉电阻点。
event->status = 1 表示全部或指定电阻点正常。
event->status = 0 表示全部或指定电阻点异常。
```

典型操作：

```plain
触觉电阻无异常：
    Touch_Event_t event = {
        .event_type = EVENT_TOUCH_STATUS,
        .touch_id = SENSOR_ID_ALL,
        .status = SENSOR_STATUS_OK,
        .timestamp_us = touch_frame_timestamp_us,
    };
    Touch_setEvent(&event);

指定电阻点异常：
    Touch_Event_t event = {
        .event_type = EVENT_TOUCH_STATUS,
        .touch_id = r_id,
        .status = SENSOR_STATUS_ERROR,
        .error_code = touch_error_code,
        .timestamp_us = touch_frame_timestamp_us,
    };
    Touch_setEvent(&event);
```

寄存器更新规则：

```plain
REG_R_STATUS_START 0x2084
REG_R_STATUS_END   0x2087

0x2084：R0  ~ R15
0x2085：R16 ~ R31
0x2086：R32 ~ R47
0x2087：R48 ~ R63

event->status = 1：置位对应 bit
event->status = 0：清零对应 bit

REG_R_TIMESTAMP_US 0x2080 ~ 0x2083：
    event->timestamp_us 写入 g_rs485.touch_timestamp_us。
```

伪代码：

```c
void Touch_setEvent(const Touch_Event_t *event)
{
    uint8_t reg_index;
    uint8_t bit_index;

    if (event == NULL)
    {
        return;
    }

    g_rs485.touch_timestamp_us = event->timestamp_us;

    if (event->event_type != EVENT_TOUCH_STATUS)
    {
        return;
    }

    if (event->touch_id == SENSOR_ID_ALL)
    {
        for (uint8_t i = 0; i < 4; i++)
        {
            g_rs485.r_status_regs[i] = event->status ? 0xFFFF : 0x0000;
        }
        return;
    }

    if (event->touch_id >= 64)
    {
        return;
    }

    reg_index = event->touch_id / 16;
    bit_index = event->touch_id % 16;

    if (event->status)
    {
        g_rs485.r_status_regs[reg_index] |= (uint16_t)(1U << bit_index);
    }
    else
    {
        g_rs485.r_status_regs[reg_index] &= (uint16_t)~(1U << bit_index);
    }
}
```

---

## 6. RS485 收发中断处理

### 6.1 RX DMA + IDLE 接收处理

接收侧采用 UART RX DMA + IDLE 中断。DMA 负责把收到的数据连续写入 `rx_buf`，IDLE 中断表示总线上出现空闲，可以认为当前 Modbus-RTU 请求帧已经结束。

初始化流程：

```plain
1. 配置 UART 波特率为 2M。
2. 配置 RX DMA 接收缓冲区 rx_buf。
3. 启动 UART RX DMA。
4. 使能 UART IDLE 中断。
5. 默认 RS485 DE 为接收态。
```

IDLE 中断处理流程：

```plain
1. UART 触发 IDLE 中断。
2. 清除 IDLE 标志。
3. 暂停或停止 RX DMA。
4. 根据 DMA 剩余计数计算本帧长度 rx_len。
5. 判断 rx_len：
   - rx_len == 0：认为是空闲误触发或 DMA 状态异常；
   - rx_len > 0：认为收到一帧待处理数据。
6. 若 rx_len == 0：
   - 停止 RX DMA；
   - 反初始化 UART；
   - 重新初始化 UART；
   - 重新配置 UART IDLE 中断；
   - 重新启动 RX DMA。
7. 若 rx_len > 0：
   - 设置 rx_pending = 1；
   - 投递 RS485_EVT_RX_DMA_IDLE 接收事件消息；
   - 由 RS485Task 开启 3.5 字符定时器后退出本轮任务处理。
8. 不在 IDLE 中断中解析 Modbus 指令。
```

函数原型：

```c
void RS485_UartIdleIRQ(void);
```

伪代码：

```c
void RS485_UartIdleIRQ(void)
{
    uint16_t remain;

    UART_ClearIdleFlag();
    DMA_RxStop();

    remain = DMA_GetRemainingCount();
    g_rs485.rx_len = MODBUS_RX_BUF_SIZE - remain;

    if (g_rs485.rx_len == 0U)
    {
        RS485_RxRecover();
        return;
    }

    if (g_rs485.rx_len > MODBUS_RX_BUF_SIZE)
    {
        g_rs485.rx_len = 0U;
        g_rs485.frame_ready = 0U;
        g_rs485.comm_state |= RS485_COMM_RX_OVERFLOW;
        RS485_RxRecover();
        return;
    }

    if (g_rs485.rx_len > 0U)
    {
        g_rs485.rx_pending = 1U;
        RS485_PostEvent(RS485_EVT_RX_DMA_IDLE);
    }
}
```

接收异常恢复函数：

```c
void RS485_RxRecover(void)
{
    DMA_RxStop();
    UART_DeInit();
    UART_Init_2Mbps();
    UART_EnableIdleIRQ();
    DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
}
```

说明：

```plain
1. rx_len == 0 时不触发 Modbus 指令解析。
2. rx_len > 0 时不立即解析，也不立即重启 DMA，避免 rx_buf 被覆盖。
3. RS485Task 收到 RS485_EVT_RX_DMA_IDLE 后只启动 3.5 字符定时器，然后退出本轮任务。
4. 3.5 字符定时器确认帧间隔后，再投递 RS485_EVT_FRAME_TIMEOUT。
5. RS485Task 收到 RS485_EVT_FRAME_TIMEOUT 后再进行指令解析、打包回复和发送。
6. TX DMA 完成中断确认发送成功并等待 TC 后，再调用 DMA_RxRestart() 进入下一帧接收。
```

UART 中断中只处理 IDLE：

```c
void USARTx_IRQHandler(void)
{
    if (UART_IsIdleIRQ())
    {
        RS485_UartIdleIRQ();
    }
}
```

注意：

```plain
1. IDLE 中断里只计算长度、异常恢复或投递接收事件。
2. CRC 校验、功能码解析、寄存器分发放到 RS485Task 中执行。
3. 如果 rx_len 超过协议允许最大长度，RS485Task 中设置 RS485_COMM_LEN_ERROR。
4. 若 DMA 出现溢出或错误，设置 RS485_COMM_RX_OVERFLOW。
```

---

### 6.2 3.5 字符定时器确认帧间隔

主接收触发来自 UART IDLE。RS485Task 收到 `RS485_EVT_RX_DMA_IDLE` 后启动 3.5 字符定时器，用于确认 Modbus-RTU 帧间隔。定时器到期后投递 `RS485_EVT_FRAME_TIMEOUT`，由 RS485Task 再进行指令解析。

流程：

```plain
1. RS485Task 收到 RS485_EVT_RX_DMA_IDLE。
2. RS485Task 启动 T3.5 定时器，然后退出本轮任务处理。
3. T3.5 定时器溢出。
4. 定时器中断停止定时器。
5. 若 rx_pending = 1 且 rx_len > 0：
   - 设置 frame_ready = 1；
   - 清除 rx_pending；
   - 投递 RS485_EVT_FRAME_TIMEOUT。
6. RS485Task 收到 RS485_EVT_FRAME_TIMEOUT 后解析完整 Modbus 帧。
```

伪代码：

```c
void RS485_FrameTimer_IRQ(void)
{
    RS485_FrameTimer_Stop();

    if ((g_rs485.rx_pending != 0U) && (g_rs485.rx_len > 0U))
    {
        g_rs485.rx_pending = 0U;
        g_rs485.frame_ready = 1U;
        RS485_PostEvent(RS485_EVT_FRAME_TIMEOUT);
    }
}
```

### 6.3 发送 DMA 完成中断

RS485 半双工发送时，需要在发送前打开发送使能，在 DMA 发送完成后关闭发送使能并恢复接收。

流程：

```plain
1. 发送前拉高 DE，使能 RS485 发送。
2. 启动 UART TX DMA。
3. DMA 完成中断触发。
4. 在发送中断中检查 DMA 发送是否成功。
5. 若发送失败：
   - 设置 REG_COMM_STATE 的 RS485_COMM_TX_ERROR 或 RS485_COMM_TX_BUSY；
   - 拉低 DE，切回接收；
   - 清理 rx_len / frame_ready / tx_busy；
   - 重启 RX DMA。
6. 若发送成功：
   - 等待 UART TC 发送完成标志，确保最后 1 byte 已移出移位寄存器；
   - 拉低 DE，切回接收；
   - tx_busy = 0；
   - 清空 rx_len 和 frame_ready；
   - 重启 RX DMA，进入下一帧接收。
```

伪代码：

```c
void RS485_TxDmaDoneIRQ(void)
{
    if (!DMA_TxIsSuccess())
    {
        g_rs485.comm_state |= RS485_COMM_TX_ERROR;
        RS485_DE_RX();
        g_rs485.tx_busy = 0U;
        g_rs485.rx_len = 0U;
        g_rs485.frame_ready = 0U;
        DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
        return;
    }

    UART_WaitTxComplete();
    RS485_DE_RX();
    g_rs485.tx_busy = 0U;
    g_rs485.rx_len = 0U;
    g_rs485.frame_ready = 0U;
    DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
}
```

---

## 7. RS485_task 事件处理流程

`RS485_task` 根据事件消息分阶段处理 RS485 通信。接收事件不直接解析指令，而是先开启 3.5 字符定时器；定时器确认帧间隔后，再进入指令解析、数据打包和 DMA 发送流程。

事件链路：

```plain
1. UART IDLE 中断：
   - 计算 rx_len；
   - rx_len == 0：恢复串口和 DMA；
   - rx_len > 0：投递 RS485_EVT_RX_DMA_IDLE。

2. RS485_task 收到 RS485_EVT_RX_DMA_IDLE：
   - 启动 3.5 字符定时器；
   - 退出本轮任务处理，不解析指令。

3. 3.5 字符定时器中断：
   - 设置 frame_ready = 1；
   - 投递 RS485_EVT_FRAME_TIMEOUT。

4. RS485_task 收到 RS485_EVT_FRAME_TIMEOUT：
   - 判断 frame_ready 和 rx_len；
   - 校验最小长度；
   - 校验 SlaveAddr；
   - 校验 CRC16；
   - 根据功能码分发：
   - 0x03：读保持寄存器；
   - 0x06：写单个寄存器；
   - 0x10：写多个寄存器。

5. 指令处理完成：
   - 根据寄存器地址执行对应操作；
   - 打包 Modbus 回复帧；
   - 启动 TX DMA 发送；
   - TX DMA 完成中断检查发送结果；
   - 等待 UART TC；
   - 清空 rx_len / frame_ready；
   - 重启 RX DMA，切回接收。
```

伪代码：

```c
void RS485_task(void *argument)
{
    uint32_t evt;

    for (;;)
    {
        evt = RS485_WaitEvent();

        if (evt & RS485_EVT_RX_DMA_IDLE)
        {
            RS485_FrameTimer_Start();
            continue;
        }

        if (evt & RS485_EVT_FRAME_TIMEOUT)
        {
            RS485_ProcessReceivedFrame();
            continue;
        }

        if (evt & RS485_EVT_TX_DMA_DONE)
        {
            RS485_TxDmaDoneIRQ();
            continue;
        }
    }
}
```

### 7.1 完整帧指令处理函数

当 `RS485_task` 收到 `RS485_EVT_FRAME_TIMEOUT` 后，调用该函数处理 Modbus 指令。

函数原型：

```c
void RS485_ProcessReceivedFrame(void);
```

处理流程：

```plain
1. 检查 frame_ready。
2. 检查 rx_len。
3. 检查帧长度至少满足 Modbus RTU 最小长度。
4. 校验 SlaveAddr 是否匹配本机地址，或是否为 0x00 地址发现。
5. 校验 CRC16。
6. 读取功能码：
   - 0x03：读保持寄存器；
   - 0x06：写单个寄存器；
   - 0x10：写多个寄存器。
7. 根据功能码解析寄存器地址和数量。
8. 根据寄存器地址触发对应操作函数。
9. 组回复帧。
10. 调用 TX DMA 发送回复。
11. 等待 TX DMA 完成中断。
12. 在发送中断中检查发送成功并等待 TC。
13. 在发送中断中清空 rx_len / frame_ready 并重启 RX DMA。
```

异常回复规则：

```plain
1. CRC 错误、地址不匹配、帧长度过短：不回复，只更新 REG_COMM_STATE。
2. 功能码不支持：返回异常帧，ExceptionCode = 0x01。
3. 寄存器地址不存在或访问范围越界：返回异常帧，ExceptionCode = 0x02。
4. 寄存器数量、ByteCount、写入值不合法：返回异常帧，ExceptionCode = 0x03。
5. 从机内部执行失败：返回异常帧，ExceptionCode = 0x04。
6. 从机忙或 TX 忙：返回异常帧，ExceptionCode = 0x06。
```

异常帧格式：

```plain
[SlaveAddr] [FuncCode | 0x80] [ExceptionCode] [CRC_L] [CRC_H]
```

功能码分发：

```plain
0x03:
    RS485_HandleReadHoldingRegs(start_reg, reg_count)

0x06:
    RS485_HandleWriteSingleReg(reg, value)

0x10:
    RS485_HandleWriteMultipleRegs(start_reg, reg_count, data)
```

寄存器地址分发示例：

```plain
0x0000:
    地址发现或读取基础寄存器。

0x000A ~ 0x000D:
    写入同步 UTC。

0x0020 ~ 0x0023:
    写入命令寄存器或读取 ACK；CMD_LOG_START/CMD_LOG_STOP、CMD_ACQ_START/CMD_ACQ_STOP 均从这里分发。

0x0092 / 0x009A:
    日志控制寄存器，触发创建文件或查询长度。

0x0081 ~ 0x00BF:
    读取 SD 状态区。

0x1140 ~ 0x1144:
    读取 IMU 时间戳与状态。

0x1154 ~ 0x1293:
    写入 IMU Offset。

0x2080 ~ 0x2087:
    读取触觉电阻时间戳与状态。
```

伪代码：

```c
void RS485_ProcessReceivedFrame(void)
{
    uint8_t func;
    uint16_t start_reg;
    uint16_t reg_count;
    uint16_t reg;
    uint16_t value;

    if (!g_rs485.frame_ready)
    {
        return;
    }

    g_rs485.frame_ready = 0;

    if (g_rs485.rx_len < MODBUS_MIN_FRAME_SIZE)
    {
        g_rs485.comm_state |= RS485_COMM_LEN_ERROR;
        g_rs485.rx_len = 0;
        g_rs485.frame_ready = 0;
        DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
        return;
    }

    if (!Modbus_CheckSlaveAddr(g_rs485.rx_buf[0]))
    {
        g_rs485.comm_state |= RS485_COMM_ADDR_ERROR;
        g_rs485.rx_len = 0;
        g_rs485.frame_ready = 0;
        DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
        return;
    }

    if (!Modbus_CheckCRC(g_rs485.rx_buf, g_rs485.rx_len))
    {
        g_rs485.comm_state |= RS485_COMM_CRC_ERROR;
        g_rs485.rx_len = 0;
        g_rs485.frame_ready = 0;
        DMA_RxRestart(g_rs485.rx_buf, MODBUS_RX_BUF_SIZE);
        return;
    }

    func = g_rs485.rx_buf[1];

    switch (func)
    {
        case MB_FC_READ_HOLDING_REGS:
            start_reg = Modbus_ReadU16(&g_rs485.rx_buf[2]);
            reg_count = Modbus_ReadU16(&g_rs485.rx_buf[4]);
            if (!RS485_HandleReadHoldingRegs(start_reg, reg_count))
            {
                RS485_BuildExceptionResponse(func, MB_EX_ILLEGAL_DATA_ADDRESS);
            }
            break;

        case MB_FC_WRITE_SINGLE_REG:
            reg = Modbus_ReadU16(&g_rs485.rx_buf[2]);
            value = Modbus_ReadU16(&g_rs485.rx_buf[4]);
            if (!RS485_HandleWriteSingleReg(reg, value))
            {
                RS485_BuildExceptionResponse(func, MB_EX_ILLEGAL_DATA_ADDRESS);
            }
            break;

        case MB_FC_WRITE_MULTIPLE_REGS:
            start_reg = Modbus_ReadU16(&g_rs485.rx_buf[2]);
            reg_count = Modbus_ReadU16(&g_rs485.rx_buf[4]);
            if (!RS485_HandleWriteMultipleRegs(start_reg, reg_count, &g_rs485.rx_buf[7]))
            {
                RS485_BuildExceptionResponse(func, MB_EX_ILLEGAL_DATA_VALUE);
            }
            break;

        default:
            g_rs485.comm_state |= RS485_COMM_FUNC_ERROR;
            RS485_BuildExceptionResponse(func, MB_EX_ILLEGAL_FUNCTION);
            break;
    }

    RS485_StartTxDma(g_rs485.tx_buf, g_rs485.tx_len);
}
```

---

## 8. 根据寄存器反馈执行操作

### 8.1 读寄存器操作

根据 `Modbus-RTU 主从通信任务流程.md` 的寄存器表返回数据：

```plain
0x0044：REG_SENSOR_STATE
0x0045：REG_COMM_STATE
0x0081 ~ 0x00BF：SD 状态区
0x1140 ~ 0x1143：REG_IMU_TIMESTAMP_US
0x1144：REG_IMU_STATUS_BITS
0x2080 ~ 0x2083：REG_R_TIMESTAMP_US
0x2084 ~ 0x2087：REG_R_STATUS_START ~ REG_R_STATUS_END
```

处理要求：

```plain
1. uint16 直接按 Modbus 大端字节序返回。
2. float32 按小端寄存器序返回。
3. uint64 按小端寄存器序返回。
4. IMU 状态来自 IMU_setEvent 更新后的 imu_status_bits。
5. 触觉电阻状态来自 Touch_setEvent 更新后的 r_status_regs[4]。
6. IMU 时间戳来自 imu_timestamp_us，按 uint64 小端寄存器序返回。
7. 触觉电阻时间戳来自 touch_timestamp_us，按 uint64 小端寄存器序返回。
```

### 8.2 写日志控制寄存器与命令

日志创建和长度查询仍按既有日志控制寄存器处理；日志开始/停止统一通过命令寄存器区处理。

```plain
CMD_LOG_CREATE_FILE 0x0092：写入 1，创建日志文件。
CMD_LOG_LENGTH      0x009A：查询指定文件长度，读取返回文件长度，占 4 regs。

CMD_LOG_START       0x0094：写入 REG_CMD，开始日志记录。
CMD_LOG_STOP        0x0096：写入 REG_CMD，停止日志记录。
```

日志控制寄存器操作函数建议：

```c
void RS485_HandleLogControl(uint16_t reg, uint16_t value)
{
    switch (reg)
    {
        case CMD_LOG_CREATE_FILE:
            if (value == 1)
            {
                SD_LogCreateFile();
            }
            break;

        default:
            break;
    }
}
```

命令寄存器中的日志命令由 `RS485_HandleCommand()` 统一分发：

```c
case CMD_LOG_START:
    SD_LogStart();
    g_rs485.cmd_ack = CMD_ACK_OK;
    break;

case CMD_LOG_STOP:
    SD_LogStop();
    g_rs485.cmd_ack = CMD_ACK_OK;
    break;
```

### 8.3 写 IMU Offset 寄存器

IMU 校准不通过命令寄存器触发。主机计算 Offset 后，直接写入：

```plain
0x1154 ~ 0x1293：REG_IMU_OFFSET_START ~ REG_IMU_OFFSET_END
```

处理要求：

```plain
1. 支持按单个 IMU 写入 20 个寄存器。
2. 每个 Offset 字段为 float32，占 2 个寄存器。
3. 使用小端寄存器序保存。
4. 写入完成后可刷新到 Flash 或本地校准参数区。
```

### 8.4 写采集控制命令

采集控制状态和命令：

```plain
REG_WORK_STATE 0x0500  // 工作状态，只读或由从机内部更新
CMD_ACQ_START  0x0501  // 写入 REG_CMD，开始采集任务
CMD_ACQ_STOP   0x0502  // 写入 REG_CMD，结束采集任务
```

处理策略：

```plain
1. 主机写 REG_CMD = CMD_ACQ_START 时，从机启动 IMU 和电阻点阵采集任务。
2. 采集启动成功后更新 REG_WORK_STATE = WORK_STATE_ACQUIRING。
3. 主机写 REG_CMD = CMD_ACQ_STOP 时，从机停止高频采集任务。
4. 采集停止成功后更新 REG_WORK_STATE = WORK_STATE_IDLE。
5. REG_WORK_STATE 用于主机读取确认，不作为主机直接写入的控制入口。
```

### 8.5 写命令寄存器

当前启用的命令寄存器：

```plain
REG_CMD       0x0020
REG_CMD_PARAM 0x0021
REG_CMD_SEQ   0x0022
REG_CMD_ACK   0x0023
REG_CMD_ACK_SEQ 0x0024
REG_CMD_ERROR   0x0025
```

已定义命令：

```plain
CMD_LOG_START        0x0094  // 开始日志记录
CMD_LOG_STOP         0x0096  // 停止日志记录
CMD_ACQ_START        0x0501  // 开始采集任务
CMD_ACQ_STOP         0x0502  // 结束采集任务
CMD_SD_LIST_FILES    TBD  // 暂未实现，命令字待重新分配
CMD_SD_DOWNLOAD_FILE TBD  // 暂未实现，命令字待重新分配
```

处理策略：

```plain
1. 收到 REG_CMD / REG_CMD_PARAM / REG_CMD_SEQ 后保存命令上下文。
2. 根据 REG_CMD 判断是否支持。
3. 当前未实现的命令返回 ACK = 0x8001 或 0x8003。
4. 已实现命令执行成功后返回 ACK = 0x0001。
5. 每次命令处理完成后，REG_CMD_ACK_SEQ 必须回显本次 REG_CMD_SEQ。
6. 命令执行失败时，REG_CMD_ERROR 返回具体原因；执行成功时 REG_CMD_ERROR = 0。
```

---

## 9. 推荐初始化流程

```plain
1. 初始化 UART，波特率 2M。
2. 初始化 RS485 DE 控制 GPIO，默认接收态。
3. 初始化 RX DMA，并使能 UART IDLE 中断。
4. 初始化 TX DMA。
5. 初始化 T3.5 帧间隔定时器，周期建议 18 us ~ 20 us，作为兜底或调试校验。
6. 初始化状态寄存器：
   - IMU 全部正常：imu_status_bits = 0xFFFF；
   - IMU 时间戳清零：imu_timestamp_us = 0；
   - 触觉电阻全部正常：r_status_regs[0..3] = 0xFFFF；
   - 触觉电阻时间戳清零：touch_timestamp_us = 0；
   - comm_state = 0；
   - sensor_state = 0。
7. 启动 UART RX DMA 接收。
```

---

## 10. 最小接口清单

```c
void RS485_EventHelper_Init(void);
void RS485_task(void *argument);
void RS485_UartIdleIRQ(void);
void RS485_RxRecover(void);
void RS485_TxDmaDoneIRQ(void);
void RS485_StartTxDma(uint8_t *buf, uint16_t len);
void RS485_FrameTimer_Start(void);
void RS485_FrameTimer_IRQ(void);
void RS485_ProcessReceivedFrame(void);

void IMU_setEvent(const IMU_Event_t *event);
void Touch_setEvent(const Touch_Event_t *event);

void RS485_HandleReadHoldingRegs(uint16_t start_reg, uint16_t reg_count);
void RS485_HandleWriteSingleReg(uint16_t reg, uint16_t value);
void RS485_HandleWriteMultipleRegs(uint16_t start_reg, uint16_t reg_count, const uint8_t *data);
```

---

## 11. 注意事项

```plain
1. 2M 波特率下 T3.5 很短，定时器中断优先级需要合理设置。
2. TX DMA 完成后必须等待 UART TC，再切回接收，避免最后字节丢失。
3. Modbus CRC 校验失败不回复，只更新通信错误状态。
4. 一问一答模式下，从机不主动上报事件，只通过寄存器读取得知状态。
5. IMU 和触觉状态位默认建议全 1，表示全部正常。
6. 任何事件函数只更新状态寄存器缓存，不直接发送 RS485 帧。
```

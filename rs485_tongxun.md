# RS485 Modbus RTU 通信测试指令

本文按 `Modbus-RTU 主从通信任务流程.md` 整理可直接发送的测试帧。默认从机地址为 `0x01`，所有帧末尾均为 Modbus RTU CRC16，低字节在前。

当前固件已实现的主要链路是 `0x03` 读保持寄存器，以及 `0x10` 写入 `REG_TIME_SYNC_UTC_US` 时间同步寄存器。命令寄存器区、SD 控制、采集控制、IMU Offset 写入等帧按流程文档列出，若当前固件尚未实现对应写入处理，可能返回异常帧。

## 1. 串口参数

| 项目 | 参数 |
| --- | --- |
| 物理层 | UART + RS485 半双工 |
| 串口 | USART2 |
| 波特率 | 3000000 |
| 数据位 | 8 |
| 校验位 | None |
| 停止位 | 1 |
| 默认从机地址 | 0x01 |

## 2. 数据格式

`uint16` 占 1 个寄存器。

`float32` 占 2 个寄存器，低 16 位寄存器在前，单个寄存器内部高字节在前。

`uint64` 占 4 个寄存器，低 16 位寄存器在前，单个寄存器内部高字节在前。

例如：

```text
raw64 = 0x1122334455667788
Reg + 0 = 0x7788
Reg + 1 = 0x5566
Reg + 2 = 0x3344
Reg + 3 = 0x1122
Modbus 数据区 = 77 88 55 66 33 44 11 22
```

## 3. 地址发现

1 对 1 通信时，主机可用广播地址 `0x00` 读取 `REG_SLAVE_ADDR`。

请求：

```text
00 03 00 00 00 01 85 DB
```

预期响应格式：

```text
00 03 02 00 01 44 44
```

响应帧地址保持 `0x00`，主机从 `REG_SLAVE_ADDR` 数据区解析出真实地址后，后续通信使用 `0x01`。

## 4. 设备状态查询

### 4.1 基础通信与时间寄存器

读取范围：`0x0000`，数量 `0x000E`

```text
01 03 00 00 00 0E C4 0E
```

读取内容：

| 地址 | 内容 |
| --- | --- |
| 0x0000 | 从站地址 |
| 0x0001 | 波特率编码 |
| 0x0002 ~ 0x0005 | 当前系统 UTC，uint64，us |
| 0x0006 ~ 0x0009 | 本地计时，uint64，us |
| 0x000A ~ 0x000D | 最近同步 UTC，uint64，us |

### 4.2 系统状态区

读取范围：`0x0040`，数量 `0x000A`

```text
01 03 00 40 00 0A C4 19
```

响应数据长度应为 `0x14`。

### 4.3 电源状态区

读取范围：`0x0060`，数量 `0x0004`

```text
01 03 00 60 00 04 44 17
```

响应数据长度应为 `0x08`。

### 4.4 SD 卡与日志状态区

读取范围：`0x0081`，数量 `0x003F`

```text
01 03 00 81 00 3F 55 F2
```

响应数据长度应为 `0x7E`。

### 4.5 IMU 状态位

读取范围：`0x1144`，数量 `0x0001`

```text
01 03 11 44 00 01 C1 23
```

`bit0 ~ bit15` 对应 `IMU0 ~ IMU15`，`1` 表示正常。

### 4.6 电阻点阵状态位

读取范围：`0x2088`，数量 `0x0009`

```text
01 03 20 88 00 09 0E 26
```

`0x2088 ~ 0x2090` 共 9 个状态寄存器，对应 `R0 ~ R131`，`1` 表示正常；`0x2090` 的 bit4 ~ bit15 保留为 0。

## 5. 时间同步

同步顺序：

1. 主机获取该 PPS/GPIO 边沿对应的 `utc_us`。
2. 主机输出 GPIO/PPS 同步边沿。
3. 从机在边沿中断中保存 TIM5 计数。
4. 主机通过 RS485 写入该边沿对应的 `utc_us`。
5. 从机收到 UTC 帧后计算误差因子，补偿 GPIO 到 RS485 帧之间的时间差，然后清零 TIM5。
6. 主机读取当前 UTC，判断时间是否合理递增。

### 5.1 写入同步 UTC

写入 `REG_TIME_SYNC_UTC_US = 0x000A`，数量 `4 regs`。

示例：`utc_us = 1718000000000000 = 0x00061A830BB96000`

```text
01 10 00 0A 00 04 08 60 00 0B B9 1A 83 00 06 42 18
```

预期响应：

```text
01 10 00 0A 00 04 E1 C8
```

### 5.2 读取当前校准 UTC

读取范围：`0x0002`，数量 `0x0004`

```text
01 03 00 02 00 04 E5 C9
```

响应格式：

```text
01 03 08 W0H W0L W1H W1L W2H W2L W3H W3L CRC_L CRC_H
```

还原方式：

```text
utc_us = word0 + (word1 << 16) + (word2 << 32) + (word3 << 48)
```

### 5.3 读取本地 TIM5 计时

读取范围：`0x0006`，数量 `0x0004`

```text
01 03 00 06 00 04 A4 08
```

用途：确认本地计时是否以 us 递增。

### 5.4 读取最近同步 UTC

读取范围：`0x000A`，数量 `0x0004`

```text
01 03 00 0A 00 04 64 0B
```

用途：确认从机最近一次收到的主站同步 UTC。

## 6. SD 日志控制

以下控制帧来自任务流程文档。当前固件若只允许写时间同步寄存器，则这些写入帧会返回异常。

### 6.1 创建日志文件

写 `0x0092 = 0x0001`：

```text
01 10 00 92 00 01 02 00 01 7B 22
```

预期响应格式：

```text
01 10 00 92 00 01 CRC_L CRC_H
```

随后读取 SD 状态：

```text
01 03 00 81 00 3F 55 F2
```

### 6.2 开始日志记录

写命令区：`REG_CMD = 0x0094`，`REG_CMD_PARAM = 0`，`REG_CMD_SEQ = 1`

```text
01 10 00 20 00 03 06 00 94 00 00 00 01 17 F7
```

读 ACK：

```text
01 03 00 23 00 03 F4 01
```

读取日志状态 `0x0082`：

```text
01 03 00 82 00 01 24 22
```

### 6.3 停止日志记录

写命令区：`REG_CMD = 0x0096`，`REG_CMD_PARAM = 0`，`REG_CMD_SEQ = 2`

```text
01 10 00 20 00 03 06 00 96 00 00 00 02 2E 36
```

读 ACK：

```text
01 03 00 23 00 03 F4 01
```

读取日志状态：

```text
01 03 00 82 00 01 24 22
```

## 7. 采集控制

以下控制帧来自任务流程文档。当前固件若只允许写时间同步寄存器，则这些写入帧会返回异常。

### 7.1 开始采集

写命令区：`REG_CMD = 0x0501`，`REG_CMD_PARAM = 0`，`REG_CMD_SEQ = 3`

```text
01 10 00 20 00 03 06 05 01 00 00 00 03 9A 7E
```

读 ACK：

```text
01 03 00 23 00 03 F4 01
```

读取工作状态 `0x0500`：

```text
01 03 05 00 00 01 84 C6
```

### 7.2 停止采集

写命令区：`REG_CMD = 0x0502`，`REG_CMD_PARAM = 0`，`REG_CMD_SEQ = 4`

```text
01 10 00 20 00 03 06 05 02 00 00 00 04 9F BC
```

读 ACK：

```text
01 03 00 23 00 03 F4 01
```

读取工作状态：

```text
01 03 05 00 00 01 84 C6
```

## 8. 高频数据读取

采集任务开始后，主机按 100 Hz 周期读取 7 帧数据。

| 帧 | 请求 | 内容 | 响应 ByteCount |
| --- | --- | --- | --- |
| Frame 0 | `01 03 10 00 00 78 41 28` | IMU float[0] ~ float[59] | 0xF0 |
| Frame 1 | `01 03 10 78 00 78 C1 31` | IMU float[60] ~ float[119] | 0xF0 |
| Frame 2 | `01 03 10 F0 00 50 41 05` | IMU float[120] ~ float[159] | 0xA0 |
| Frame 3 | `01 03 1F D6 00 2A 22 39` | joint_angle[0] ~ joint_angle[20] | 0x54 |
| Frame 4 | `01 03 20 00 00 3C 4E 1B` | R_ADC[0] ~ R_ADC[59] | 0x78 |
| Frame 5 | `01 03 20 3C 00 3C 8E 17` | R_ADC[60] ~ R_ADC[119] | 0x78 |
| Frame 6 | `01 03 20 78 00 0C CE 16` | R_ADC[120] ~ R_ADC[131] | 0x18 |

时间戳读取：

```text
IMU 时间戳：01 03 11 40 00 04 40 E1
R 时间戳：  01 03 20 84 00 04 0F E0
```

解析要求：

```text
1. 校验 CRC。
2. 检查功能码为 0x03。
3. 检查 ByteCount 是否等于寄存器数量 * 2。
4. IMU 和关节角度的 float32 按低 16 位寄存器在前解析。
5. 电阻点阵按 uint16 ADC 原始值解析，不转换为 float。
6. 7 帧拼接后得到 160 个 IMU float、21 个关节角度 float 和 132 个电阻点阵 ADC 值。
```

## 9. 低频监控建议

高频采集期间建议低频轮询以下帧：

| 频率 | 内容 | 请求 |
| --- | --- | --- |
| 1 Hz | SD 状态 | `01 03 00 81 00 3F 55 F2` |
| 1 Hz | 系统状态 | `01 03 00 40 00 0A C4 19` |
| 1 Hz | 电源状态 | `01 03 00 60 00 04 44 17` |
| 10 Hz | IMU 状态 | `01 03 11 44 00 01 C1 23` |
| 10 Hz | 电阻点阵状态 | `01 03 20 88 00 09 0E 26` |
| 10 Hz | 当前 UTC | `01 03 00 02 00 04 E5 C9` |

## 10. Python 发送示例

```python
import serial

ser = serial.Serial("COM3", 3000000, bytesize=8, parity="N", stopbits=1, timeout=0.2)

def send(hex_text, read_len=256):
    frame = bytes.fromhex(hex_text)
    ser.write(frame)
    resp = ser.read(read_len)
    print(resp.hex(" ").upper())
    return resp

send("00 03 00 00 00 01 85 DB", 16)
send("01 03 00 40 00 0A C4 19", 64)
send("01 03 00 02 00 04 E5 C9", 32)
```

## 11. 推荐联调顺序

1. 地址发现：发送 `00 03 00 00 00 01 85 DB`。
2. 状态查询：读取基础时间、系统状态、IMU 状态、电阻点阵状态。
3. 时间同步：GPIO/PPS 边沿后写入 UTC64，再读取当前 UTC。
4. SD/日志控制：按流程发送创建文件、开始日志、查询 ACK 和日志状态。
5. 采集控制：发送开始采集，查询 ACK 和工作状态。
6. 高频读取：按 7 帧循环读取 IMU、关节角度和电阻点阵。
7. 停止流程：停止采集，停止日志，确认状态回到空闲。

## 12. 读指令响应实例

本节给出所有读指令的具体响应示例和响应 CRC。动态值采用以下示例数据：

```text
utc_us        = 1718000000000000 = 0x00061A830BB96000
local_uptime  = 123456789        = 0x00000000075BCD15
temperature  = 25.0f             = 0x41C80000
```

### 12.1 地址发现响应

请求：

```text
00 03 00 00 00 01 85 DB
```

响应示例：

```text
00 03 02 00 01 44 44
```

含义：回复帧地址保持 `0x00`，数据区 `REG_SLAVE_ADDR = 0x0001`，真实从机地址为 `0x01`。

### 12.2 基础通信与时间寄存器响应

请求：

```text
01 03 00 00 00 0E C4 0E
```

响应示例：

```text
01 03 1C 00 01 00 00 60 00 0B B9 1A 83 00 06 CD 15 07 5B 00 00 00 00 60 00 0B B9 1A 83 00 06 65 25
```

解析：

| 字段 | 示例值 |
| --- | --- |
| `REG_SLAVE_ADDR` | `0x0001` |
| `REG_BAUDRATE_CODE` | `0x0000` |
| `REG_UTC_TIMESTAMP_US` | `1718000000000000 us` |
| `REG_LOCAL_UPTIME_US` | `123456789 us` |
| `REG_TIME_SYNC_UTC_US` | `1718000000000000 us` |

### 12.3 系统状态区响应

请求：

```text
01 03 00 40 00 0A C4 19
```

响应示例：

```text
01 03 14 00 01 00 00 00 00 00 00 FF FF 00 01 00 00 00 00 00 00 41 C8 C6 CF
```

解析：

| 字段 | 示例值 |
| --- | --- |
| `REG_SYSTEM_STATE` | `0x0001` |
| `REG_WORK_MODE` | `0x0000` |
| `REG_LOG_STATE` | `0x0000` |
| `REG_SD_STATE` | `0x0000` |
| `REG_SENSOR_STATE` | `0xFFFF` |
| `REG_COMM_STATE` | `0x0001` |
| `REG_SYSTEM_RESERVED` | `0x0000 0x0000` |
| `REG_TEMPERATURE_BOARD` | `25.0f` |

### 12.4 电源状态区响应

请求：

```text
01 03 00 60 00 04 44 17
```

响应示例：

```text
01 03 08 00 00 00 00 00 00 00 00 95 D7
```

解析：电池电压 `0.0f`，电池电流 `0.0f`。

### 12.5 SD 卡与日志状态区响应

请求：

```text
01 03 00 81 00 3F 55 F2
```

响应示例，63 个寄存器均为 `0x0000`：

```text
01 03 7E
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
00 00 00 00 00 00 00 00 00 00 00 00 00 00
47 83
```

### 12.6 IMU 状态响应

请求：

```text
01 03 11 44 00 01 C1 23
```

响应示例：

```text
01 03 02 FF FF B9 F4
```

含义：`IMU0 ~ IMU15` 状态位均为 `1`。

### 12.7 电阻点阵状态响应

请求：

```text
01 03 20 88 00 09 0E 26
```

响应示例：

```text
01 03 12 FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF FF 00 0F 82 A2
```

含义：`R0 ~ R131` 状态位均为 `1`，最后一个状态寄存器 bit4 ~ bit15 保留为 0。

### 12.8 当前 UTC 响应

请求：

```text
01 03 00 02 00 04 E5 C9
```

响应示例：

```text
01 03 08 60 00 0B B9 1A 83 00 06 79 AD
```

含义：`utc_us = 1718000000000000`。

### 12.9 本地计时响应

请求：

```text
01 03 00 06 00 04 A4 08
```

响应示例：

```text
01 03 08 CD 15 07 5B 00 00 00 00 78 A5
```

含义：`local_uptime = 123456789 us`。

### 12.10 最近同步 UTC 响应

请求：

```text
01 03 00 0A 00 04 64 0B
```

响应示例：

```text
01 03 08 60 00 0B B9 1A 83 00 06 79 AD
```

含义：最近一次同步 UTC 为 `1718000000000000 us`。

### 12.11 命令 ACK 响应

请求：

```text
01 03 00 23 00 03 F4 01
```

响应示例：

```text
01 03 06 00 00 00 00 00 00 21 75
```

解析：

| 字段 | 示例值 |
| --- | --- |
| `REG_CMD_ACK` | `0x0000` |
| `REG_CMD_ACK_SEQ` | `0x0000` |
| `REG_CMD_ERROR` | `0x0000` |

### 12.12 日志状态响应

请求：

```text
01 03 00 82 00 01 24 22
```

响应示例：

```text
01 03 02 00 00 B8 44
```

含义：日志状态为空闲。

### 12.13 工作状态响应

请求：

```text
01 03 05 00 00 01 84 C6
```

响应示例：

```text
01 03 02 00 00 B8 44
```

含义：工作状态为空闲。

### 12.14 IMU 时间戳响应

请求：

```text
01 03 11 40 00 04 40 E1
```

响应示例：

```text
01 03 08 60 00 0B B9 1A 83 00 06 79 AD
```

### 12.15 电阻点阵时间戳响应

请求：

```text
01 03 20 84 00 04 0F E0
```

响应示例：

```text
01 03 08 60 00 0B B9 1A 83 00 06 79 AD
```

### 12.16 高频数据读取响应

按照流程文档，高频数据区正常实现后应返回完整数据帧；如果当前固件尚未开放 `0x1000 ~ 0x113F`、`0x1FD6 ~ 0x1FFF` 和 `0x2000 ~ 0x2083` 数据区，可能返回非法地址异常：

```text
01 83 02 C0 F1
```

协议完整实现时，若数据区全为 0，各帧响应示例如下。

Frame 0 请求：

```text
01 03 10 00 00 78 41 28
```

Frame 0 全零响应，`ByteCount = 0xF0`，CRC 为 `8C DB`：

```text
01 03 F0 [240 bytes of 00] 8C DB
```

Frame 1 请求：

```text
01 03 10 78 00 78 C1 31
```

Frame 1 全零响应，`ByteCount = 0xF0`，CRC 为 `8C DB`：

```text
01 03 F0 [240 bytes of 00] 8C DB
```

Frame 2 请求：

```text
01 03 10 F0 00 50 41 05
```

Frame 2 全零响应，`ByteCount = 0xA0`，CRC 为 `A5 89`：

```text
01 03 A0 [160 bytes of 00] A5 89
```

Frame 3 请求：

```text
01 03 1F D6 00 2A 22 39
```

Frame 3 全零响应，`ByteCount = 0x54`，CRC 为 `99 95`：

```text
01 03 54 [84 bytes of 00] 99 95
```

Frame 4 请求：

```text
01 03 20 00 00 3C 4E 1B
```

Frame 4 全零响应，`ByteCount = 0x78`，CRC 为 `D1 07`：

```text
01 03 78 [120 bytes of 00] D1 07
```

Frame 5 请求：

```text
01 03 20 3C 00 3C 8E 17
```

Frame 5 全零响应，`ByteCount = 0x78`，CRC 为 `D1 07`：

```text
01 03 78 [120 bytes of 00] D1 07
```

Frame 6 请求：

```text
01 03 20 78 00 0C CE 16
```

Frame 6 全零响应，`ByteCount = 0x18`，CRC 为 `6C F4`：

```text
01 03 18 [24 bytes of 00] 6C F4
```

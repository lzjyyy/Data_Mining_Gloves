# RS485 主机时间戳测试操作说明

本文档说明当前主机程序如何连接 RS485 从机、如何启动时间戳同步测试，以及如何通过 LCD 判断测试结果。

## 1. 测试目的

主机用于验证从机的 UTC 时间戳功能是否正常：

1. 主机通过 RS485/Modbus 向从机写入一个初始 UTC 时间戳。
2. 主机周期性读取从机当前 UTC 时间戳。
3. 主机判断相邻两次 UTC 读数的差值是否接近 100 ms。
4. 主机同时输出同步脉冲，用于观察或给从机做外部同步输入。

当前测试期望：

```text
读数周期：100 ms
期望 UTC 增量：100000 us
允许误差：+/-10000 us
从机地址：0x01
```

对应代码位置：

```c
#define SLAVE_TIME_TEST_ADDR                 MODBUS_SLAVE_ADDR_DEFAULT
#define SLAVE_TIME_TEST_INITIAL_UTC_US       1710000000000000ULL
#define SLAVE_TIME_TEST_INTERVAL_US          100000ULL
#define SLAVE_TIME_TEST_INTERVAL_TOL_US      10000ULL
#define SLAVE_TIME_TEST_MODBUS_TIMEOUT_MS    30U
```

## 2. RS485 接线

主机通过 USART2 + RS485 收发器与从机通信。

基本接线如下：

| 主机 | 从机 | 说明 |
|---|---|---|
| RS485 A | RS485 A | 差分总线 A |
| RS485 B | RS485 B | 差分总线 B |
| GND | GND | 必须共地 |

注意事项：

1. 主从必须共地，否则通信容易不稳定。
2. 如果一直没有响应，可以尝试交换 A/B。
3. 总线较长时，两端建议加 120 欧终端电阻。
4. 主机和从机串口参数必须一致。
5. 当前主机默认从机地址是 `0x01`。

主机方向控制脚：

```text
RS485_EN_Pin
接收电平：GPIO_PIN_RESET
发送电平：GPIO_PIN_SET
```

也就是主机发送前拉高方向脚，发送完成后切回接收。

## 3. 同步脉冲接线

主机会在 `time_tongbu_Pin` 输出同步脉冲。

如果从机需要外部同步输入，可以连接：

| 主机 | 从机 |
|---|---|
| time_tongbu_Pin | 从机同步输入脚 |
| GND | GND |

当前脉冲逻辑：

```text
空闲电平：高电平
运行时：约每 100 ms 输出一次低脉冲
低电平宽度：约 2 ms
```

如果只测试 Modbus 时间戳读写，不使用外部同步输入，这根同步线可以不接。

## 4. 按键启动测试

主机板上的 `KEY` 按键用于启动或停止测试。

当前按键链路：

```text
KEY -> PC13 / syctime_key_Pin -> EXTI13 -> SyncOutput_OnButtonExti()
```

按一次：

```text
STATE:RUN
开始输出同步脉冲
开始执行时间戳测试
```

再按一次：

```text
STATE:STOP
停止同步脉冲
停止本轮测试
```

LCD 上的 `KEY` 是按键切换次数，`P` 是已经输出的同步脉冲次数。

## 5. 测试流程

启动测试后，主机内部流程如下：

```text
按键启动
  |
  +-- 输出 100 ms 周期同步脉冲
  |
  +-- Modbus 写从机 REG_TIME_SYNC_UTC_US
  |      写入值：1710000000000000 us
  |
  +-- 每 100 ms 读取从机 REG_UTC_TIMESTAMP_US
  |
  +-- 计算本次 UTC - 上次 UTC
  |
  +-- 判断差值是否在 100000 us +/-10000 us 内
```

使用到的寄存器：

| 寄存器 | 地址 | 方向 | 含义 |
|---|---:|---|---|
| `REG_UTC_TIMESTAMP_US` | `0x0002` | 主机读 | 从机当前 UTC 时间戳，单位 us |
| `REG_TIME_SYNC_UTC_US` | `0x000A` | 主机写 | 主机下发的同步 UTC，单位 us |

U64 数据占 4 个 Modbus 寄存器。

## 6. LCD 显示说明

当前 LCD 主要显示四行调试信息：

```text
STATE:RUN OP:2 MS:0 ML:13
SYNC O:1 F:0 RD O:118 F:0
INTV O:117 F:0 D:100000 us
KEY:1 P:118 A:1 F:3
```

字段说明：

| 字段 | 含义 |
|---|---|
| `STATE` | 当前测试状态，`RUN` 为运行，`STOP` 为停止 |
| `OP` | 最后一次 Modbus 操作，`1` 为写同步，`2` 为读 UTC |
| `MS` | Modbus 主机状态，`0` 为成功 |
| `ML` | 最后一次回包长度 |
| `SYNC O` | 写同步 UTC 成功次数 |
| `SYNC F` | 写同步 UTC 失败次数 |
| `RD O` | 读取从机 UTC 成功次数 |
| `RD F` | 读取从机 UTC 失败次数 |
| `INTV O` | UTC 增量符合要求的次数 |
| `INTV F` | UTC 增量不符合要求的次数 |
| `D` | 最近一次相邻 UTC 差值，单位 us |
| `KEY` | 按键切换次数 |
| `P` | 同步脉冲输出次数 |
| `A` | 最后一次回包的 Modbus 地址 |
| `F` | 最后一次回包的 Modbus 功能码 |

`MS` 状态码：

| MS | 含义 |
|---:|---|
| 0 | Modbus 操作成功 |
| 1 | 等待响应超时 |
| 2 | 响应长度或 CRC 错误 |
| 3 | 地址、功能码或寄存器字段不匹配 |
| 4 | 参数错误 |

常见功能码：

| F | 含义 |
|---:|---|
| 3 | 读保持寄存器响应 |
| 16 | 写多个保持寄存器响应 |
| 0x83 | 读保持寄存器异常响应 |
| 0x90 | 写多个保持寄存器异常响应 |

## 7. 如何判断测试通过

测试通过时，LCD 应接近下面状态：

```text
STATE:RUN
MS:0
SYNC O >= 1
SYNC F = 0
RD O 持续增加
RD F = 0
INTV O 持续增加
INTV F = 0
D 接近 100000 us
A = 1
F = 3 或 16
```

其中最重要的是：

```text
RD O 持续增加
INTV O 持续增加
D 接近 100000 us
```

这说明主机能稳定读到从机 UTC，并且从机时间戳按 100 ms 周期递增。

## 8. 常见异常判断

### 8.1 按键无反应

现象：

```text
KEY 不增加
STATE 不变化
P 不增加
```

检查：

1. 是否烧录最新固件。
2. `EXTI13_IRQHandler()` 是否存在。
3. `gpio.c` 是否使能 `EXTI13_IRQn`。
4. PC13 按键按下时是否产生有效电平边沿。

关键代码应包含：

```c
void EXTI13_IRQHandler(void)
{
  HAL_GPIO_EXTI_IRQHandler(syctime_key_Pin);
}
```

### 8.2 有脉冲但没有同步成功

现象：

```text
P 持续增加
SYNC O 不增加
SYNC F 增加
```

检查：

1. RS485 A/B 是否接反。
2. 主从 GND 是否共地。
3. 从机地址是否为 `0x01`。
4. 从机是否支持写 `REG_TIME_SYNC_UTC_US`。
5. 看 `MS/ML/A/F` 判断回包是否正确。

### 8.3 同步成功但读 UTC 失败

现象：

```text
SYNC O >= 1
RD O 不增加
RD F 增加
```

检查：

1. 从机是否支持读 `REG_UTC_TIMESTAMP_US`。
2. 回包长度是否为 `13`。
3. `MS` 是否为 `1/2/3`。
4. `A` 是否为 `1`，`F` 是否为 `3`。

### 8.4 能读 UTC，但时间戳不递增

现象：

```text
SYNC O >= 1
RD O 持续增加
RD F = 0
INTV O = 0
INTV F 持续增加
D = 0 us 或明显不接近 100000 us
```

这说明通信已经正常，问题在从机时间戳更新逻辑。

检查从机：

1. 收到 `REG_TIME_SYNC_UTC_US` 后是否真正设置 UTC 基准。
2. `REG_UTC_TIMESTAMP_US` 返回值是否随本地 tick 增加。
3. U64 寄存器顺序是否和主机一致。
4. 从机时间源是否正常运行。

### 8.5 D 不等于 100000 us

如果 `D` 接近但不完全等于 100000 us，比如：

```text
99000 us ~ 101000 us
```

通常可以接受。

如果 `D` 长期明显偏离，比如：

```text
0 us
10000 us
200000 us
```

则分别可能是：

| D | 可能原因 |
|---:|---|
| 0 | 从机 UTC 没有递增 |
| 10000 | 主机读取周期或从机更新周期是 10 ms |
| 200000 | 主机或从机有丢周期/阻塞 |

## 9. 当前主机侧已验证现象

当 LCD 出现类似：

```text
SYNC O:1 F:0
RD O:118 F:0
OP:2 MS:0 ML:13
A:1 F:3
```

表示：

```text
按键 OK
RS485 发送 OK
从机响应 OK
写同步 OK
读 UTC OK
```

如果此时：

```text
INTV O:0
INTV F:117
D:0 us
```

则不是主机通信问题，而是从机返回的 UTC 没有递增。

## 10. 相关代码文件

| 文件 | 作用 |
|---|---|
| `Core/Src/gpio.c` | PC13 按键、RS485_EN、同步输出 GPIO 初始化 |
| `Core/Src/stm32h5xx_it.c` | EXTI13、USART2、TIM5、TIM6 中断入口 |
| `Core/Src/main.c` | EXTI 回调、TIM 回调、外设初始化 |
| `task/src/sync_output_task.c` | 按键启停与同步脉冲输出 |
| `task/src/slave_time_test_task.c` | 主机时间戳测试主逻辑 |
| `task/src/lcd_task.c` | LCD 状态显示 |
| `APP/rs485_app/RS485_uasrt.c` | RS485 DMA 收发和方向控制 |
| `APP/modbus_app/modbus_master.c` | Modbus 主机读写 U64 |
| `APP/modbus_app/modbus_registers.h` | Modbus 寄存器地址定义 |


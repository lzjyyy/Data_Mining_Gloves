手动安装 Paho MQTT C 与 C++ 库（推荐方法）
1. 安装依赖
sudo apt install cmake build-essential

2. 编译安装 Paho MQTT C 库：
git clone https://github.com/eclipse/paho.mqtt.c.git
cd paho.mqtt.c
cmake -Bbuild -H. -DPAHO_WITH_SSL=TRUE
sudo cmake --build build/ --target install
sudo ldconfig
这一步会在系统里安装：

头文件到 /usr/local/include/paho-mqtt

库文件到 /usr/local/lib/libpaho-mqtt3c.so、libpaho-mqtt3a.so

3. 编译安装 Paho MQTT C++ 库：
cd ~
git clone https://github.com/eclipse/paho.mqtt.cpp.git
cd paho.mqtt.cpp
cmake -Bbuild -H. -DPAHO_WITH_SSL=TRUE -DPAHO_MQTT_C_PATH=/usr/local
sudo cmake --build build/ --target install
sudo ldconfig
这一步安装：

头文件到 /usr/local/include/paho-mqttpp3

库文件到 /usr/local/lib/libpaho-mqttpp3.so

nlohmann_json（JSON for Modern C++）库安装
sudo apt update
sudo apt install nlohmann-json3-dev
安装后，CMake 会自动在 /usr/lib/x86_64-linux-gnu/cmake/nlohmann_json/

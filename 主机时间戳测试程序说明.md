# 主机时间戳测试程序说明

本文档说明当前工程作为 RS485/Modbus 主机时，用来测试从机 UTC 时间戳同步与递增是否正常的程序流程。

## 1. 程序目标

主机程序完成三件事：

1. 通过按键启动或停止同步测试。
2. 启动后周期性输出同步脉冲，并通过 Modbus 向从机写入一个初始 UTC 时间戳。
3. 周期性读取从机返回的 UTC 时间戳，判断相邻两次读数的增量是否接近 100 ms。

当前测试使用的关键参数在 `task/src/slave_time_test_task.c` 中定义：

```c
#define SLAVE_TIME_TEST_ADDR                 MODBUS_SLAVE_ADDR_DEFAULT
#define SLAVE_TIME_TEST_INITIAL_UTC_US       1710000000000000ULL
#define SLAVE_TIME_TEST_INTERVAL_US          100000ULL
#define SLAVE_TIME_TEST_INTERVAL_TOL_US      10000ULL
#define SLAVE_TIME_TEST_MODBUS_TIMEOUT_MS    30U
```

含义如下：

| 参数 | 含义 |
|---|---|
| `SLAVE_TIME_TEST_ADDR` | 从机地址，默认 `0x01` |
| `SLAVE_TIME_TEST_INITIAL_UTC_US` | 主机写给从机的初始 UTC，单位 us |
| `SLAVE_TIME_TEST_INTERVAL_US` | 期望从机时间戳每次增加 `100000 us`，即 100 ms |
| `SLAVE_TIME_TEST_INTERVAL_TOL_US` | 允许误差 `10000 us`，即 +/-10 ms |
| `SLAVE_TIME_TEST_MODBUS_TIMEOUT_MS` | 单次 Modbus 请求等待超时 30 ms |

## 2. 硬件和外设关系

| 功能 | 外设/引脚 | 代码位置 |
|---|---|---|
| RS485 通讯 | USART2 + DMA + `RS485_EN_Pin` | `APP/rs485_app/RS485_uasrt.c` |
| 同步脉冲输出 | `time_tongbu_Pin` | `task/src/sync_output_task.c` |
| 启停按键/外部触发 | `syctime_key_Pin`，EXTI13 上升沿 | `Core/Src/gpio.c`、`Core/Src/main.c` |
| LCD 状态显示 | SPI1 LCD | `task/src/lcd_task.c` |
| 测试读取事件 | TIM5 更新中断 | `APP/timers_app/timers_APP.c` |
| LCD 刷新事件 | TIM6 更新中断 | `APP/timers_app/timers_APP.c` |

## 3. 启动流程

主程序初始化顺序在 `Core/Src/main.c`：

```text
HAL_Init
SystemClock_Config
MX_GPIO_Init
MX_GPDMA1_Init / MX_GPDMA2_Init
MX_ICACHE_Init
MX_RTC_Init
MX_SPI1_Init
MX_USART2_UART_Init
MX_TIM6_Init
MX_TIM5_Init
LCD 初始化和初始界面显示
RS485_Init
Timers_APP_Init
osKernelInitialize
MX_FREERTOS_Init
osKernelStart
```

`RS485_Init()` 会先通过 RS485 发出一段启动字符串 `RS485 RUN\r\n`，然后切回接收模式，并启动 UART To-Idle DMA 接收。

`Timers_APP_Init()` 启动 TIM5 和 TIM6 的中断。TIM5 用于产生从机时间戳读取事件，TIM6 用于产生 LCD 刷新事件。

FreeRTOS 任务由 `AppTask_Init()` 创建，主要任务如下：

| 任务 | 优先级 | 作用 |
|---|---|---|
| `rs485Task` | `osPriorityBelowNormal` | 处理 RS485 发送完成事件和接收帧通知 |
| `lcdTask` | `osPriorityLow` | 刷新 LCD 调试信息 |
| `slaveTimeTest` | `osPriorityNormal` | 执行从机时间戳同步和读取测试 |
| `syncOutput` | `osPriorityNormal` | 按键启停同步脉冲输出 |

## 4. 按键启停和同步脉冲

按键 `syctime_key_Pin` 配置为上升沿中断：

```text
EXTI13_IRQHandler
  -> HAL_GPIO_EXTI_IRQHandler(syctime_key_Pin)
    -> HAL_GPIO_EXTI_Rising_Callback
      -> SyncOutput_OnButtonExti(GPIO_Pin)
```

`SyncOutput_OnButtonExti()` 负责：

1. 判断是否为 `syctime_key_Pin`。
2. 做 80 ms 软件消抖。
3. 翻转 `sync_output_running` 状态。
4. 累加按键切换计数 `sync_output_toggle_count`。
5. 唤醒 `syncOutput` 任务。

`syncOutput` 任务运行逻辑：

```text
未运行：
  time_tongbu_Pin 保持高电平
  等待按键事件

运行中：
  输出一次下降沿同步脉冲
  低电平保持 2 ms
  恢复高电平
  等待 100 ms 或新的按键事件
```

也就是说，测试启动后，主机会在 `time_tongbu_Pin` 上输出约 100 ms 周期的低脉冲。当前代码的同步脉冲是应用层软件延时产生的，精度受 RTOS 调度和中断影响。

## 5. 时间戳测试任务流程

核心逻辑在 `StartSlaveTimeTestTask()`。

任务启动后先延时 300 ms，等待系统其它部分稳定。之后一直检查 `SyncOutput_IsRunning()`：

```text
如果同步输出未运行：
  清除本轮测试状态
  继续等待

如果同步输出刚启动：
  清除 last_utc、sync_sent 等状态

如果尚未给从机写入初始 UTC：
  写 REG_TIME_SYNC_UTC_US = 1710000000000000
  写入成功则 sync_ok++
  写入失败则 sync_fail++，100 ms 后重试

等待 TIM5 产生 slave_time_check_event

事件到来后：
  读取 REG_UTC_TIMESTAMP_US
  读取成功则 read_ok++
  读取失败则 read_fail++

如果已有上一次 UTC：
  delta = current_utc - last_utc
  判断 delta 是否在 100 ms +/-10 ms 内
  符合则 interval_ok++
  不符合则 interval_fail++

保存 current_utc 作为 last_utc
```

简化时序如下：

```text
按键启动
  |
  +-- syncOutput 输出 100 ms 周期同步脉冲
  |
  +-- slaveTimeTest 写从机 REG_TIME_SYNC_UTC_US
  |
  +-- TIM5 事件到来
        |
        +-- 读 REG_UTC_TIMESTAMP_US
        +-- 和上次读数比较
        +-- 更新 ok/fail 计数
```

## 6. Modbus 寄存器和数据格式

寄存器定义在 `APP/modbus_app/modbus_registers.h`：

| 寄存器 | 地址 | 宽度 | 方向 | 用途 |
|---|---:|---:|---|---|
| `REG_UTC_TIMESTAMP_US` | `0x0002` | U64，4 个寄存器 | 主机读 | 从机当前 UTC 时间戳，单位 us |
| `REG_TIME_SYNC_UTC_US` | `0x000A` | U64，4 个寄存器 | 主机写 | 主机下发给从机的同步 UTC，单位 us |

主机使用两个 Modbus 功能码：

| 操作 | 功能码 | 函数 |
|---|---|---|
| 写 U64 | `MB_FC_WRITE_MULTIPLE_REGS`，即写多个保持寄存器 | `ModbusMaster_WriteU64()` |
| 读 U64 | `MB_FC_READ_HOLDING_REGS`，即读保持寄存器 | `ModbusMaster_ReadU64()` |

U64 被拆成 4 个 16 bit Modbus 寄存器。当前代码按低 16 bit 在前的顺序放入数据区：

```c
for (index = 0U; index < MODBUS_REGS_U64; index++)
{
  ModbusMaster_WriteU16(&data[index * 2U],
                        (uint16_t)((value >> (index * 16U)) & 0xFFFFU));
}
```

注意：每个 16 bit 寄存器内部仍然是 Modbus 常见的大端字节序，即高字节在前；但 64 bit 值的寄存器顺序是低字在前。主从两端必须保持一致。

## 7. RS485 通讯机制

RS485 使用 USART2 + DMA，方向由 `RS485_EN_Pin` 控制：

```text
发送前：
  停止接收
  RS485_EN 切到发送方向
  启动 UART DMA 发送

发送完成：
  USART TC 回调通知 rs485Task
  rs485Task 调用 RS485_ProcessTxEvent
  清 tx_busy
  RS485_EN 切回接收方向
  重新启动 ReceiveToIdle DMA

接收帧：
  HAL_UARTEx_RxEventCallback 记录帧长度和 ready 标志
  RS485_TakeRxFrame 取走帧并重新启动接收
```

主机请求的收发过程在 `ModbusMaster_SendAndWait()`：

1. 清空旧 RX 帧。
2. 等待 RS485 不忙。
3. 发送请求帧。
4. 等待发送完成。
5. 在超时时间内等待从机响应帧。
6. 校验长度、地址、功能码和 CRC。

## 8. LCD 显示含义

LCD 任务周期刷新以下信息：

```text
STATE:RUN/STOP IRQ:x CB:x
SYNC O:x F:x RD O:x F:x
INTV O:x F:x D:x us
KEY:x PULSE:x TE:x RX:x
```

字段含义：

| 字段 | 含义 |
|---|---|
| `STATE` | 当前测试是否运行，来自 `SyncOutput_IsRunning()` |
| `IRQ` | TX DMA IRQ 计数 |
| `CB` | UART TX complete 回调计数 |
| `SYNC O` | 写入初始 UTC 成功次数 |
| `SYNC F` | 写入初始 UTC 失败次数 |
| `RD O` | 读取从机 UTC 成功次数 |
| `RD F` | 读取从机 UTC 失败次数 |
| `INTV O` | 相邻 UTC 增量符合 100 ms +/-10 ms 的次数 |
| `INTV F` | 相邻 UTC 增量不符合要求的次数 |
| `D` | 最近一次相邻 UTC 差值，单位 us |
| `KEY` | 按键切换次数 |
| `PULSE` | 已输出同步脉冲次数 |
| `TE` | RS485 任务收到的 UART 发送完成事件数 |
| `RX` | RS485 接收帧事件数 |

## 9. 判定方法

测试通过时，通常应看到：

```text
STATE:RUN
SYNC O 持续为 1 或偶尔增加，SYNC F 不增加
RD O 持续增加，RD F 不增加或很少增加
INTV O 持续增加，INTV F 不增加
D 接近 100000 us
PULSE 持续增加
RX 和 TE 随通讯增加
```

异常判断：

| 现象 | 可能原因 |
|---|---|
| `STATE` 无法切到 `RUN` | 按键/EXTI13 未触发，或上升沿电平不符合 |
| `PULSE` 不增加 | `syncOutput` 任务未运行，或按键状态未切换成功 |
| `SYNC F` 增加 | 从机未响应、地址错误、RS485 接线/方向控制异常、CRC 或寄存器不匹配 |
| `RD F` 增加 | 读取超时、从机未实现 `REG_UTC_TIMESTAMP_US`、通讯不稳定 |
| `INTV F` 增加 | 从机时间戳递增周期不对、主从 U64 字序不一致、读取触发周期不对 |
| `D` 明显不是 100000 us | 检查 TIM5 周期、从机时间基准、同步脉冲处理逻辑 |

## 10. 当前代码需要重点确认的地方

1. `slaveTimeTest` 期望相邻读数间隔为 `100000 us`，但读取触发事件来自 TIM5 更新中断。
2. 当前 `Core/Src/tim.c` 中 TIM5 配置为：

```c
htim5.Init.Prescaler = 250-1;
htim5.Init.Period = 4294967295;
```

在常见 250 MHz 定时器时钟下，TIM5 计数频率约为 1 MHz，溢出周期约为 4294 秒，并不是 100 ms。因此如果目标是每 100 ms 读取一次从机 UTC，需要把 TIM5 配置成 100 ms 周期，或者改用 `syncOutput` 的 100 ms 周期来触发读取事件。

3. `syncOutput` 使用 `osDelay()` 产生脉冲，适合功能测试；如果后续要测高精度时间同步，建议改为硬件定时器输出比较/PWM 或定时器触发 GPIO。
4. `ModbusTimeSync_OnGpioFalling()` 和一些同步误差计算接口目前为空实现，当前主机测试主要依赖“写 UTC + 读 UTC 增量”来判断从机时间戳是否工作。

## 11. 相关文件索引

| 文件 | 作用 |
|---|---|
| `Core/Src/main.c` | 外设初始化、RS485 初始化、定时器启动、EXTI 和 TIM 回调入口 |
| `Core/Src/stm32h5xx_it.c` | EXTI13、TIM5、TIM6、USART2、DMA 中断入口 |
| `task/src/app_task.c` | 创建 FreeRTOS 应用任务 |
| `task/src/sync_output_task.c` | 按键启停和同步脉冲输出 |
| `task/src/slave_time_test_task.c` | 主机时间戳测试主逻辑 |
| `task/src/rs485_task.c` | RS485 事件任务 |
| `task/src/lcd_task.c` | LCD 状态显示 |
| `APP/rs485_app/RS485_uasrt.c` | RS485 UART DMA 收发和方向控制 |
| `APP/modbus_app/modbus_master.c` | Modbus 主机读写 U64 |
| `APP/modbus_app/modbus_registers.h` | Modbus 地址和寄存器表 |
| `APP/timers_app/timers_APP.c` | TIM5/TIM6 事件分发 |
